
本文件夹中的文件来自于文件夹caideng_ORL数据，其中
ORL中包含两个数据
	fea 1024 * 400 每列一个样本(1024=32*32)
	gnd   1    * 400 每列一个标签
这个Yale数据包含40类，每类有10个样本

文件　idxData_k,　k = 2,...,8　包含对400个样本的随机分割
每个文件中包含两个数据
	idxTrain_k 50 x (k*40)		每行对应一个分割，每个分割含　k*40　 个样本组成训练样本
	idxTest_k  50 x (10-k)*40	每行对应一个分割，每个分割含(10-k)*40个样本组成测试样本

